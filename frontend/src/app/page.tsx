import CurtainPage from '@/components/Landing/CurtainPage'; // Adjusted import path using alias
import Box from '@mui/material/Box';

export default function HomePage() {
  return (
    <Box> {/* Optional wrapping Box */}
      <CurtainPage />
    </Box>
  );
}
